# Get length of the square from the user
length = int(input("Enter the length: "))

# Calculate the area of the square using the formula: area = length * length
area = length * length

# print the calculated area to the console
print(f"The area of the square with length {length} is {area}")
